import React from 'react';

export default function AnnouncementBar() {
  return (
    <div className="w-full bg-brand-espresso text-brand-cream text-center py-2 px-4 text-[10px] sm:text-xs uppercase tracking-[0.2em] font-semibold">
      Fast shipping updates • Order support available 24/7
    </div>
  );
}
